# 重要安全警示 SECURITY WARNING
项目：website_manager 本地网址管理工具

## 核心高危风险告知（必看）
1. 所有存储数据**明文无加密**
程序将你录入的 Key/Value（网址、账号、备注）直接保存至 list/*.txt 纯文本文件，无任何加密、混淆处理。
任何能访问本机文件的用户/软件均可直接打开读取全部内容。
严禁录入：网站登录密码、支付账号、身份证、手机号、私人隐私等敏感信息。

2. 程序无网络加密传输，但本地文件存在泄露风险
本工具无任何联网上传、同步云端逻辑，不会主动外传数据；但本地 txt 文件极易被拷贝、窃取。

3. 第三方修改可植入恶意程序
仅推荐自行从本仓库源码编译 webm.exe。
网络下载他人打包好的 exe 存在篡改风险，可能附加窃取本地文件、记录终端输入的恶意逻辑。

4. 无访问权限校验
程序不会设置文件保护，Windows 同设备其他账户均可读取 list 目录数据；公共电脑使用后务必删除全部列表文件。

## 安全使用强制规范
### 允许存储内容
各类公开网站链接、工具地址、无隐私的标签名称。

### 禁止存储内容
各类账号密码、验证码、证件信息、财务相关数据、私人通讯账号。

## 降低风险操作建议
1. 数据隔离
专门新建独立列表仅存放公开网址，不混入任何个人隐私信息。
2. 本地文件防护
右键 `list` 文件夹 → 属性-安全，移除其他用户读取权限；使用压缩加密包备份数据。
3. 环境规范
不在网吧、机房、公共共享电脑长期留存本程序与数据文件。
4. 编译可信校验
使用官方仓库源码 + MinGW 自行编译，拒绝来历不明的 webm.exe。
5. 定期清理
闲置、更换设备时，彻底删除 list 文件夹与 listConfig.txt。

## 漏洞上报
若发现程序越权读写、文件溢出等代码安全缺陷，请参照 SECURITY.md 规范提交安全报告，请勿公开披露漏洞利用方式。