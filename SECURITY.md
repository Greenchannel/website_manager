# 安全策略 Security Policy
本项目：website_manager
轻量本地网址管理工具，C++23 开发，Windows 终端运行，明文本地文本存储数据。

## 1. 支持版本
仅维护仓库最新 main 分支源码，旧编译二进制文件不提供安全补丁，请自行拉取最新源码重新编译 webm.exe。

| 版本状态 | 支持情况 |
|--------|--------|
| main 主线最新代码 | ✅ 接收安全修复 |
| 历史打包 exe 旧版本 | ❌ 不再维护 |

## 2. 已知安全限制（重要提示）
1. **数据明文存储**
所有网址、账号键值保存在 `list/*.txt` 纯文本，无加密，请勿存储银行卡、支付密码、私密账号等高敏感信息。
2. 程序无权限隔离
编译后的 webm.exe 读取/写入本地文件，Windows 用户拥有文件读写权限即可修改数据。
3. 无网络请求功能
本工具**不发起任何网络请求**，不会上传本地数据至云端，不存在数据外发风险。
4. 终端输入无脱敏
add 命令输入的 value 会完整写入本地文件，日志仅记录操作行为，不打印完整存储内容。

## 3. 漏洞报告方式
### 3.1 普通安全缺陷（代码逻辑、文件读写、终端输出）
1. 新建 GitHub Issue，标签标记 `security`；
2. 附带：复现步骤、系统环境（Win10/11、PowerShell版本）、漏洞影响范围。

### 3. 高危隐私/代码漏洞（隐私泄露、文件越权读写等）
不公开 Issue，联系仓库所有者 Greenchannel 或仓库管理 qingchuanhub 私下沟通漏洞细节，确认修复后统一公示。

## 4. 漏洞响应流程
1. 接收漏洞报告：36小时内确认收到并回复(中国寒暑假)；
2. 漏洞评估：区分低危/中危/高危；
3. 修复开发：低危7日内、高危4日内提交修复代码；
4. 发布更新：合并修复至 main 分支，同步更新 Wiki 安全提示；
5. 漏洞公示：高危漏洞修复完成后公开说明风险与解决方案。

## 5. 安全最佳实践（用户须知）
1. 数据保护
存放普通网址即可，密码、隐私信息禁止录入工具；如需保存敏感数据，请自行对 list 文件夹加密压缩。
2. 文件权限
Windows 右键 `list` 文件夹 → 属性，限制其他用户读取权限，防止本地他人查看存储内容。
3. 程序可信性
仅编译本仓库开源源码得到 webm.exe，不要使用第三方分发的未知 exe，避免篡改植入恶意代码。
4. 定期备份
定期复制 `list` 文件夹、listConfig.txt 备份，防止文件丢失。
5. 终端使用
公共电脑使用完成后，删除对应列表文件，清空本地存储数据。

## 6. 不视为安全漏洞的场景
1. 设计上明文存储数据（文档已提前告知）；
2. Windows 系统本身文件权限导致的本地文件读取；
3. 用户手动泄露 txt 文件产生的信息泄露；
4. 第三方编译器/系统环境带来的底层风险。

## 7. 依赖安全维护
项目依赖：CLI11（命令行解析）、spdlog（日志库）
1. 定期更新头文件依赖至稳定版本，修复依赖库安全漏洞；
2. 若上游依赖爆出高危漏洞，会尽快替换/升级依赖并推送修复。
